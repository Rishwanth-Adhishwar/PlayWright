import { test } from "@playwright/test";

test.describe('Authentications', { tag: ['@smoke', '@sanity'] }, () => {
    test('login', async ({ page }) => { });
    test('logout', async ({ page }) => { });
})